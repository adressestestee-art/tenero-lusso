// js/menu.js

// Ce script est pour un menu qui pourrait avoir besoin de manipulation JavaScript
// Par exemple pour un menu hamburger sur mobile, ou pour des effets d'ouverture/fermeture.
// Pour l'instant, on peut le laisser vide si ton menu est simple.

// Exemple de code pour un menu hamburger
/*
document.addEventListener('DOMContentLoaded', function() {
    const hamburger = document.querySelector('.hamburger-menu');
    const navLinks = document.querySelector('.nav-links');

    if (hamburger && navLinks) {
        hamburger.addEventListener('click', function() {
            navLinks.classList.toggle('active');
        });
    }
});
*/
