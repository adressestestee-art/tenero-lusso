<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URBAN - Streetwear Minimal</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: #fafafa;
            color: #111;
            line-height: 1.5;
        }

        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(20px);
            z-index: 1000;
            border-bottom: 1px solid #eee;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 1rem 2rem;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 600;
            color: #111;
            text-decoration: none;
            letter-spacing: 0.5px;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            color: #666;
            text-decoration: none;
            font-weight: 400;
            transition: color 0.2s ease;
        }

        .nav-links a:hover {
            color: #111;
        }

        .cart-icon {
            position: relative;
            cursor: pointer;
            font-size: 1.2rem;
            color: #111;
            padding: 0.5rem;
            transition: opacity 0.2s ease;
        }

        .cart-icon:hover {
            opacity: 0.7;
        }

        .cart-count {
            position: absolute;
            top: -2px;
            right: -2px;
            background: #111;
            color: white;
            border-radius: 50%;
            width: 18px;
            height: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
            font-weight: 500;
        }

        main {
            margin-top: 80px;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
            padding: 0 2rem;
        }

        .hero {
            text-align: center;
            padding: 4rem 0;
            margin-bottom: 3rem;
        }

        .hero h1 {
            font-size: 2.5rem;
            font-weight: 300;
            margin-bottom: 1rem;
            color: #111;
            letter-spacing: -0.5px;
        }

        .hero p {
            font-size: 1rem;
            color: #666;
            max-width: 500px;
            margin: 0 auto;
            font-weight: 400;
        }

        .filters {
            display: flex;
            justify-content: center;
            gap: 0;
            margin-bottom: 4rem;
            border-bottom: 1px solid #eee;
        }

        .filter-btn {
            background: none;
            color: #666;
            border: none;
            padding: 1rem 1.5rem;
            cursor: pointer;
            transition: color 0.2s ease;
            font-size: 0.9rem;
            font-weight: 400;
            border-bottom: 2px solid transparent;
        }

        .filter-btn:hover {
            color: #111;
        }

        .filter-btn.active {
            color: #111;
            border-bottom-color: #111;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 3rem;
            margin-bottom: 4rem;
        }

        .product-card {
            background: white;
            transition: all 0.2s ease;
        }

        .product-card:hover {
            transform: translateY(-2px);
        }

        .product-image {
            width: 100%;
            height: 300px;
            background: #f8f8f8;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .product-info {
            padding: 0;
        }

        .product-title {
            font-size: 1rem;
            font-weight: 500;
            margin-bottom: 0.25rem;
            color: #111;
        }

        .product-category {
            color: #999;
            font-size: 0.85rem;
            margin-bottom: 1rem;
            font-weight: 400;
        }

        .product-price {
            font-size: 1.1rem;
            font-weight: 500;
            color: #111;
            margin-bottom: 1rem;
        }

        .add-to-cart {
            width: 100%;
            background: #111;
            color: white;
            border: none;
            padding: 0.75rem;
            cursor: pointer;
            font-weight: 400;
            transition: all 0.2s ease;
            font-size: 0.9rem;
        }

        .add-to-cart:hover {
            background: #333;
        }

        .cart-sidebar {
            position: fixed;
            top: 0;
            right: -400px;
            width: 400px;
            height: 100vh;
            background: white;
            z-index: 2000;
            transition: right 0.3s ease;
            padding: 2rem;
            overflow-y: auto;
            border-left: 1px solid #eee;
        }

        .cart-sidebar.open {
            right: 0;
        }

        .cart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #eee;
        }

        .cart-header h2 {
            color: #111;
            font-size: 1.25rem;
            font-weight: 500;
        }

        .close-cart {
            background: none;
            border: none;
            color: #666;
            font-size: 1.5rem;
            cursor: pointer;
            transition: color 0.2s ease;
            padding: 0.25rem;
        }

        .close-cart:hover {
            color: #111;
        }

        .cart-item {
            display: flex;
            gap: 1rem;
            margin-bottom: 1.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid #f0f0f0;
        }

        .cart-item-image {
            width: 60px;
            height: 60px;
            background: #f8f8f8;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
        }

        .cart-item-info {
            flex: 1;
        }

        .cart-item-title {
            color: #111;
            font-weight: 500;
            font-size: 0.9rem;
            margin-bottom: 0.25rem;
        }

        .cart-item-price {
            color: #111;
            font-weight: 500;
            font-size: 0.9rem;
        }

        .quantity-controls {
            display: flex;
            gap: 0.75rem;
            align-items: center;
            margin-top: 0.75rem;
        }

        .quantity-btn {
            background: #f8f8f8;
            color: #111;
            border: none;
            width: 28px;
            height: 28px;
            cursor: pointer;
            transition: background 0.2s ease;
            font-size: 0.8rem;
        }

        .quantity-btn:hover {
            background: #eee;
        }

        .quantity-btn.remove {
            background: #111;
            color: white;
        }

        .quantity-btn.remove:hover {
            background: #333;
        }

        .quantity {
            color: #111;
            font-size: 0.9rem;
            min-width: 20px;
            text-align: center;
        }

        .cart-total {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid #eee;
        }

        .total-amount {
            font-size: 1.5rem;
            font-weight: 500;
            color: #111;
            margin-bottom: 1.5rem;
            text-align: center;
        }

        .checkout-btn {
            width: 100%;
            background: #111;
            color: white;
            border: none;
            padding: 1rem;
            cursor: pointer;
            font-weight: 400;
            font-size: 1rem;
            transition: background 0.2s ease;
        }

        .checkout-btn:hover {
            background: #333;
        }

        .cart-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.1);
            z-index: 1500;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .cart-overlay.open {
            opacity: 1;
            visibility: visible;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
            color: #999;
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            nav {
                padding: 1rem;
            }

            main {
                padding: 0 1rem;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .products-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .cart-sidebar {
                width: 100%;
                right: -100%;
            }

            .filters {
                overflow-x: auto;
                justify-content: flex-start;
                gap: 0;
                -webkit-overflow-scrolling: touch;
            }

            .filter-btn {
                white-space: nowrap;
                flex-shrink: 0;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="#" class="logo">URBAN</a>
            <ul class="nav-links">
                <li><a href="#accueil">Accueil</a></li>
                <li><a href="#hommes">Hommes</a></li>
                <li><a href="#femmes">Femmes</a></li>
                <li><a href="#accessoires">Accessoires</a></li>
            </ul>
            <div class="cart-icon" onclick="toggleCart()">
                ⁝⁞⁝
                <span class="cart-count" id="cart-count">0</span>
            </div>
        </nav>
    </header>

    <main>
        <section class="hero">
            <h1>Streetwear Essentials</h1>
            <p>Collection minimaliste de vêtements streetwear premium pour un style urbain intemporel.</p>
        </section>

        <section class="filters">
            <button class="filter-btn active" data-category="tous">Tous</button>
            <button class="filter-btn" data-category="hoodies">Hoodies</button>
            <button class="filter-btn" data-category="t-shirts">T-Shirts</button>
            <button class="filter-btn" data-category="pantalons">Pantalons</button>
            <button class="filter-btn" data-category="sneakers">Sneakers</button>
            <button class="filter-btn" data-category="accessoires">Accessoires</button>
        </section>

        <section class="products-grid" id="products-grid">
            <!-- Les produits seront générés par JavaScript -->
        </section>
    </main>

    <div class="cart-overlay" id="cart-overlay" onclick="toggleCart()"></div>
    <div class="cart-sidebar" id="cart-sidebar">
        <div class="cart-header">
            <h2>Panier</h2>
            <button class="close-cart" onclick="toggleCart()">×</button>
        </div>
        <div id="cart-items"></div>
        <div class="cart-total">
            <div class="total-amount" id="total-amount">0€</div>
            <button class="checkout-btn" onclick="checkout()">Commander</button>
        </div>
    </div>

    <script>
        // Données des produits
        const products = [
            {
                id: 1,
                name: "Hoodie Oversized",
                category: "hoodies",
                price: 89,
                emoji: "👕",
                description: "Coton biologique premium"
            },
            {
                id: 2,
                name: "T-Shirt Essential",
                category: "t-shirts",
                price: 45,
                emoji: "👔",
                description: "Coupe classique moderne"
            },
            {
                id: 3,
                name: "Jogger Minimal",
                category: "pantalons",
                price: 75,
                emoji: "👖",
                description: "Coupe ajustée confortable"
            },
            {
                id: 4,
                name: "Sneakers Clean",
                category: "sneakers",
                price: 120,
                emoji: "👟",
                description: "Design épuré intemporel"
            },
            {
                id: 5,
                name: "Casquette Basic",
                category: "accessoires",
                price: 35,
                emoji: "🧢",
                description: "Six panneaux structurés"
            },
            {
                id: 6,
                name: "Hoodie Tech",
                category: "hoodies",
                price: 95,
                emoji: "👕",
                description: "Coupe moderne technique"
            },
            {
                id: 7,
                name: "T-Shirt Organic",
                category: "t-shirts",
                price: 38,
                emoji: "👔",
                description: "100% coton biologique"
            },
            {
                id: 8,
                name: "Pantalon Wide",
                category: "pantalons",
                price: 68,
                emoji: "👖",
                description: "Coupe ample moderne"
            },
            {
                id: 9,
                name: "Sneakers Low",
                category: "sneakers",
                price: 98,
                emoji: "👟",
                description: "Profil bas élégant"
            },
            {
                id: 10,
                name: "Sac Crossbody",
                category: "accessoires",
                price: 55,
                emoji: "🎒",
                description: "Bandoulière ajustable"
            }
        ];

        let cart = [];
        let currentFilter = 'tous';

        // Afficher les produits
        function displayProducts(productsToShow = products) {
            const grid = document.getElementById('products-grid');
            grid.innerHTML = '';

            productsToShow.forEach(product => {
                const productCard = document.createElement('div');
                productCard.className = 'product-card';
                productCard.innerHTML = `
                    <div class="product-image">${product.emoji}</div>
                    <div class="product-info">
                        <h3 class="product-title">${product.name}</h3>
                        <p class="product-category">${product.description}</p>
                        <div class="product-price">${product.price}€</div>
                        <button class="add-to-cart" onclick="addToCart(${product.id})">
                            Ajouter
                        </button>
                    </div>
                `;
                grid.appendChild(productCard);
            });
        }

        // Filtrer les produits
        function filterProducts(category) {
            currentFilter = category;
            const filteredProducts = category === 'tous' 
                ? products 
                : products.filter(product => product.category === category);
            
            displayProducts(filteredProducts);

            // Mettre à jour les boutons de filtre
            document.querySelectorAll('.filter-btn').forEach(btn => {
                btn.classList.remove('active');
            });
            document.querySelector(`[data-category="${category}"]`).classList.add('active');
        }

        // Ajouter au panier
        function addToCart(productId) {
            const product = products.find(p => p.id === productId);
            const existingItem = cart.find(item => item.id === productId);

            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({ ...product, quantity: 1 });
            }

            updateCartDisplay();
            updateCartCount();
        }

        // Mettre à jour l'affichage du panier
        function updateCartDisplay() {
            const cartItems = document.getElementById('cart-items');
            cartItems.innerHTML = '';

            if (cart.length === 0) {
                cartItems.innerHTML = '<div class="empty-state">Votre panier est vide</div>';
                updateTotal();
                return;
            }

            cart.forEach(item => {
                const cartItem = document.createElement('div');
                cartItem.className = 'cart-item';
                cartItem.innerHTML = `
                    <div class="cart-item-image">${item.emoji}</div>
                    <div class="cart-item-info">
                        <div class="cart-item-title">${item.name}</div>
                        <div class="cart-item-price">${item.price}€</div>
                        <div class="quantity-controls">
                            <button class="quantity-btn" onclick="updateQuantity(${item.id}, -1)">−</button>
                            <span class="quantity">${item.quantity}</span>
                            <button class="quantity-btn" onclick="updateQuantity(${item.id}, 1)">+</button>
                            <button class="quantity-btn remove" onclick="removeFromCart(${item.id})">×</button>
                        </div>
                    </div>
                `;
                cartItems.appendChild(cartItem);
            });

            updateTotal();
        }

        // Mettre à jour la quantité
        function updateQuantity(productId, change) {
            const item = cart.find(item => item.id === productId);
            if (item) {
                item.quantity += change;
                if (item.quantity <= 0) {
                    removeFromCart(productId);
                } else {
                    updateCartDisplay();
                    updateCartCount();
                }
            }
        }

        // Supprimer du panier
        function removeFromCart(productId) {
            cart = cart.filter(item => item.id !== productId);
            updateCartDisplay();
            updateCartCount();
        }

        // Mettre à jour le total
        function updateTotal() {
            const total = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            document.getElementById('total-amount').textContent = total + '€';
        }

        // Mettre à jour le compteur du panier
        function updateCartCount() {
            const count = cart.reduce((sum, item) => sum + item.quantity, 0);
            const cartCountElement = document.getElementById('cart-count');
            cartCountElement.textContent = count;
            cartCountElement.style.display = count > 0 ? 'flex' : 'none';
        }

        // Basculer l'affichage du panier
        function toggleCart() {
            const sidebar = document.getElementById('cart-sidebar');
            const overlay = document.getElementById('cart-overlay');
            
            sidebar.classList.toggle('open');
            overlay.classList.toggle('open');
        }

        // Commander
        function checkout() {
            if (cart.length === 0) {
                alert('Votre panier est vide');
                return;
            }

            const total = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            alert(`Commande confirmée - Total: ${total}€`);
            
            // Vider le panier après commande
            cart = [];
            updateCartDisplay();
            updateCartCount();
            toggleCart();
        }

        // Événements
        document.addEventListener('DOMContentLoaded', function() {
            displayProducts();
            updateCartCount(); // Cacher le compteur initial

            // Événements de filtrage
            document.querySelectorAll('.filter-btn').forEach(btn => {
                btn.addEventListener('click', function() {
                    const category = this.getAttribute('data-category');
                    filterProducts(category);
                });
            });

            // Fermer le panier avec Escape
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape' && document.getElementById('cart-sidebar').classList.contains('open')) {
                    toggleCart();
                }
            });
        });
    </script>
</body>
</html>
